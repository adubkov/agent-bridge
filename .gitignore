agent-bridge-mcp
*.out
.antigravitycli/
# Generated by `make install-codex`: the canonical skills live in ./skills/ and are
# copied here at install time because Codex requires a plugin's skills to live inside
# the plugin root (its validator forbids `..`/symlink escapes).
/plugins/agent-bridge/skills/
